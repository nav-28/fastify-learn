// this should be in .env file
const config = {
  mysql: {
    host: "localhost",
    user: "fastify_user",
    password: "mysqlpassword",
    database: "fastify_db",
    port: 3306,
  },
};

export default config;
